# @package _global_

model:
  name: transformer
  params:
    d_model: 96   # Reduced from 128 for ~300K params
    nhead: 8
    num_encoder_layers: 3  # Reduced from 4
    num_decoder_layers: 3  # Reduced from 4
    dim_feedforward: 384   # Reduced from 512
    dropout: 0.1
    activation: "gelu"
    causal: true
