# @package _global_

model:
  name: nhits
  params:
    pool_sizes: [1, 2, 3]
    blocks_per_stack: 1
    hidden_size: 256
    num_layers: 2
    pred_len: 1
    dropout: 0.0
    interpolation_mode: linear
